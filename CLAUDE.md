# Local Agent Constraints
- Keep all explanations under 3 sentences.
- Never write boilerplate code; output only the exact lines that need to be changed.
- If a script requires a new environment variable, check `.env.example` before creating it.
- Always use `docker compose` for local services.
- Never execute dangerous shell commands (`rm -rf`, `chmod 777`) without explicit confirmation.
